steps:
- name: 'gcr.io/cloud-builders/gcloud'
  args:
  - functions
  - deploy
  - transfert_lucca
  - --runtime
  - Python 3.12
  - --trigger-http
  - --allow-unauthenticated
  - --entry-point
  - main
  - --source
  - .
